//Notacion

// ; > Delimitar el final de una linea
const b = 4;
b + 4
    //se puede usar sin el ; pero lo mejor es utilizarlo siempre


// . > Se utiliza en los objetos para acceder a los atributos
    //movil.anchura

// [] ->los corchetes se utilizan para las listas, arreglos o arrays
const ar = [1, 2, 3, 4]
    //Tambien se utilizada para acceder a algun valor dentro del array
console.log(ar[2])

// () >Se utilizan para funciones
function suma(a, b) {
    //Aqui se escribe la funcion
}

// {} >Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}
if (true) {
    //todo lo que haya entre llaves


    const constante2="hola"
}
